# complicated conditionals

int main() {
    int a;
    if (1 == 1 or not True) {
        int x = 0;
        if (x <> 0 and x < 10 and x >= 3){
            return "nope";
        } else {
            # just empty
        }

        int y = 1;
        if (y <> x) {
        }
        if (not True == False){
            print("not is applied first, so not True -> False");
            if (True) {
                a = 42;
                print(a);
            }
        }
    }
    return 0;
}
